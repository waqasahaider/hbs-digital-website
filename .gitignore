# Secrets — env files live only on the server, never in git
.env
infra/.env
*.env
!.env.example
!infra/.env.example

# Let's Encrypt / TLS material (managed on the server)
infra/traefik/acme.json
**/acme.json

# OS / editor cruft
.DS_Store
*.log
Thumbs.db

# Node (for a future CRM/API package)
node_modules/
dist/
