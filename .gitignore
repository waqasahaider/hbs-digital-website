# OS / editor cruft
.DS_Store
*.log
Thumbs.db

# Secrets (this static site needs none; future services keep their own
# .env in /docker/<service>/ on the VPS, never in git)
.env
*.env
!*.env.example

# Node (for a future CRM/API package)
node_modules/
dist/
